{% extends 'base.html.twig' %}

{% block body %}

    {% for movie in movies %}
        <article id="{{ movie.id }}">
            <h1>{{ movie.title }}</h1>

        </article>
    {% endfor %}
        
{% endblock %}
